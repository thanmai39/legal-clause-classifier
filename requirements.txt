# --- Data pipeline (Stage 2) ---
datasets==5.0.1
pandas==3.0.5
scikit-learn==1.9.0
python-dotenv==1.2.3

# --- Model training (Stage 3) ---
torch==2.14.0
# NOTE: downgraded from 5.16.1 -- optimum (needed for ONNX export, Stage 10)
# does not yet support transformers 5.x.
transformers==4.57.6
accelerate==1.14.0
evaluate==0.4.6
matplotlib==3.11.1

# --- API (Stage 4) ---
fastapi==0.141.1
uvicorn[standard]==0.52.4

# --- Frontend (Stage 5) ---
streamlit==1.63.0
requests==2.34.2
pypdf==6.17.0
python-docx==1.2.0

# --- Gemini explanation (Stage 6) ---
google-genai==2.22.0

# --- Deployment: ONNX export + loading from Hugging Face Hub (Stage 10) ---
huggingface-hub==0.36.2
optimum[onnxruntime]==2.1.0
onnxruntime==1.29.0

# --- Testing (Stage 8) ---
pytest==9.1.1
